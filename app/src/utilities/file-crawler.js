const path = require('path');
const fs = require('fs-extra');

class FileCrawler {

  constructor(directory) {
    this.dir = directory;
  }

  getFiles(directory) {
    const dir = this.dir;
  }

  createFile(fileName) {
    const dir = this.dir;
  }

}
