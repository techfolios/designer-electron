const path = require('path');
const fs = require('fs-extra');

class YAMLParser {

  constructor(file) {
    this.file = file;
  }

  read() {
    const file = this.file;
  }

  write() {
    const file = this.file;
  }

}
